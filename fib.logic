%vars(A,B,C,D,E,F,G,H).
bit(Z, b0, Y).
bit(Z, b1, Y).

bstr(scons(X,Y)) :- bit(c1, X, c3),bstr(Y).


%conn(X,Y).
%conn(X,Y) :- edge(X,Z),conn(Z,Y).
%edge(a,b).
%conn(b,c).

%p(f(g(f(X)))) :- p(f(f(X))).

%? :- bstr(scons(b0,Y)).
%? :- conn(a,c).


%1 -> (10, 11)


%10 -> (010, 110)
